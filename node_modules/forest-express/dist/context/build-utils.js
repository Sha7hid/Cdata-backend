"use strict";

var errorMessages = require('../utils/error-messages');
var errorUtils = require('../utils/error');
var stringUtils = require('../utils/string');
var joinUrl = require('../utils/join-url');
var _require = require('../utils/widgets'),
  setFieldWidget = _require.setFieldWidget;
module.exports = function (context) {
  return context.addInstance('errorMessages', function () {
    return errorMessages;
  }).addInstance('stringUtils', function () {
    return stringUtils;
  }).addInstance('errorUtils', function () {
    return errorUtils;
  }).addInstance('setFieldWidget', function () {
    return setFieldWidget;
  }).addInstance('joinUrl', function () {
    return joinUrl;
  });
};